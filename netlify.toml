[build]
  command = "npm run build"
  publish = "dist"

# Backup redirect (processed after public/_redirects)
[[redirects]]
  from = "/*"
  to = "https://ankitbansal.in/:splat"
  status = 301
  force = true
  conditions = { Host = ["ankit-bansal.netlify.app"] }
